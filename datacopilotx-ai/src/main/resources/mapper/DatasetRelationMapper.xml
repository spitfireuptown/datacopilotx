<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.datacopilotx.ai.mapper.DatasetRelationMapper">
    
    <select id="selectByDatasetId" resultType="com.datacopilotx.ai.domian.bean.DatasetRelationBean">
        SELECT * FROM DATA_SET_RELATION 
        WHERE dataset_id = #{datasetId} AND is_del = 0
    </select>
    
    <update id="deleteByDatasetId">
        UPDATE DATA_SET_RELATION 
        SET is_del = 1, utime = NOW() 
        WHERE dataset_id = #{datasetId}
    </update>
    
</mapper>
