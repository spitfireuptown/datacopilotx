[
  {"id": 1, "question": "有哪些门店？", "gold_sql": "SELECT shop_name FROM dims_shop", "difficulty": "simple", "tags": ["单表查询"]},
  {"id": 2, "question": "华东区有哪些门店？", "gold_sql": "SELECT shop_name FROM dims_shop WHERE region='华东'", "difficulty": "simple", "tags": ["单表查询", "条件过滤"]},
  {"id": 3, "question": "北京的门店叫什么？", "gold_sql": "SELECT shop_name FROM dims_shop WHERE city='北京'", "difficulty": "simple", "tags": ["单表查询", "条件过滤"]},
  {"id": 4, "question": "上海正大广场店的店长是谁？", "gold_sql": "SELECT manager FROM dims_shop WHERE shop_name='上海正大广场店'", "difficulty": "simple", "tags": ["单表查询", "条件过滤"]},
  {"id": 5, "question": "所有商品的名称和价格", "gold_sql": "SELECT product_name, price FROM dims_product", "difficulty": "simple", "tags": ["单表查询"]},
  {"id": 6, "question": "电子数码类商品有哪些？", "gold_sql": "SELECT product_name FROM dims_product WHERE category='电子数码'", "difficulty": "simple", "tags": ["单表查询", "条件过滤"]},
  {"id": 7, "question": "手机品类的商品", "gold_sql": "SELECT product_name FROM dims_product WHERE subcategory='手机'", "difficulty": "simple", "tags": ["单表查询", "条件过滤"]},
  {"id": 8, "question": "价格大于5000的商品", "gold_sql": "SELECT product_name, price FROM dims_product WHERE price > 5000", "difficulty": "simple", "tags": ["单表查询", "数值过滤"]},
  {"id": 9, "question": "成本低于200的商品", "gold_sql": "SELECT product_name, cost FROM dims_product WHERE cost < 200", "difficulty": "simple", "tags": ["单表查询", "数值过滤"]},
  {"id": 10, "question": "有多少个门店？", "gold_sql": "SELECT COUNT(*) FROM dims_shop", "difficulty": "simple", "tags": ["单表查询", "聚合函数"]},
  {"id": 11, "question": "有多少种商品？", "gold_sql": "SELECT COUNT(*) FROM dims_product", "difficulty": "simple", "tags": ["单表查询", "聚合函数"]},
  {"id": 12, "question": "总共有多少笔销售记录？", "gold_sql": "SELECT COUNT(*) FROM facts_sales", "difficulty": "simple", "tags": ["单表查询", "聚合函数"]},
  {"id": 13, "question": "6月1日的销售记录", "gold_sql": "SELECT * FROM facts_sales WHERE DATE(sale_time)='2026-06-01'", "difficulty": "simple", "tags": ["单表查询", "时间函数"]},
  {"id": 14, "question": "订单号为ORD10001的销售详情", "gold_sql": "SELECT * FROM facts_sales WHERE order_id='ORD10001'", "difficulty": "simple", "tags": ["单表查询", "条件过滤"]},
  {"id": 15, "question": "S001门店的销售记录", "gold_sql": "SELECT * FROM facts_sales WHERE shop_id='S001'", "difficulty": "simple", "tags": ["单表查询", "条件过滤"]},
  {"id": 16, "question": "P001商品的销售记录", "gold_sql": "SELECT * FROM facts_sales WHERE product_id='P001'", "difficulty": "simple", "tags": ["单表查询", "条件过滤"]},
  {"id": 17, "question": "销售数量大于3的记录", "gold_sql": "SELECT * FROM facts_sales WHERE quantity > 3", "difficulty": "simple", "tags": ["单表查询", "数值过滤"]},
  {"id": 18, "question": "有折扣的销售记录", "gold_sql": "SELECT * FROM facts_sales WHERE discount_amount > 0", "difficulty": "simple", "tags": ["单表查询", "数值过滤"]},
  {"id": 19, "question": "实付金额最高的销售记录", "gold_sql": "SELECT * FROM facts_sales ORDER BY total_amount DESC LIMIT 1", "difficulty": "simple", "tags": ["单表查询", "排序"]},
  {"id": 20, "question": "实付金额最低的销售记录", "gold_sql": "SELECT * FROM facts_sales ORDER BY total_amount ASC LIMIT 1", "difficulty": "simple", "tags": ["单表查询", "排序"]},
  {"id": 21, "question": "各区域有多少门店？", "gold_sql": "SELECT region, COUNT(*) FROM dims_shop GROUP BY region", "difficulty": "medium", "tags": ["聚合分组"]},
  {"id": 22, "question": "各品类有多少商品？", "gold_sql": "SELECT category, COUNT(*) FROM dims_product GROUP BY category", "difficulty": "medium", "tags": ["聚合分组"]},
  {"id": 23, "question": "各门店的销售总额", "gold_sql": "SELECT shop_id, SUM(total_amount) FROM facts_sales GROUP BY shop_id", "difficulty": "medium", "tags": ["聚合分组", "求和"]},
  {"id": 24, "question": "各商品的销售总量", "gold_sql": "SELECT product_id, SUM(quantity) FROM facts_sales GROUP BY product_id", "difficulty": "medium", "tags": ["聚合分组", "求和"]},
  {"id": 25, "question": "各门店的订单数量", "gold_sql": "SELECT shop_id, COUNT(DISTINCT order_id) FROM facts_sales GROUP BY shop_id", "difficulty": "medium", "tags": ["聚合分组", "去重计数"]},
  {"id": 26, "question": "各商品的平均售价", "gold_sql": "SELECT product_id, AVG(price) FROM facts_sales GROUP BY product_id", "difficulty": "medium", "tags": ["聚合分组", "平均值"]},
  {"id": 27, "question": "各品类商品的平均价格", "gold_sql": "SELECT category, AVG(price) FROM dims_product GROUP BY category", "difficulty": "medium", "tags": ["聚合分组", "平均值"]},
  {"id": 28, "question": "各门店的最大单笔销售额", "gold_sql": "SELECT shop_id, MAX(total_amount) FROM facts_sales GROUP BY shop_id", "difficulty": "medium", "tags": ["聚合分组", "最大值"]},
  {"id": 29, "question": "各门店的最小单笔销售额", "gold_sql": "SELECT shop_id, MIN(total_amount) FROM facts_sales GROUP BY shop_id", "difficulty": "medium", "tags": ["聚合分组", "最小值"]},
  {"id": 30, "question": "6月份每天的销售额", "gold_sql": "SELECT DATE(sale_time), SUM(total_amount) FROM facts_sales WHERE sale_time >= '2026-06-01' AND sale_time < '2026-07-01' GROUP BY DATE(sale_time)", "difficulty": "medium", "tags": ["时间函数", "聚合分组"]},
  {"id": 31, "question": "7月份每天的销售额", "gold_sql": "SELECT DATE(sale_time), SUM(total_amount) FROM facts_sales WHERE sale_time >= '2026-07-01' AND sale_time < '2026-08-01' GROUP BY DATE(sale_time)", "difficulty": "medium", "tags": ["时间函数", "聚合分组"]},
  {"id": 32, "question": "每个小时的销售笔数", "gold_sql": "SELECT HOUR(sale_time), COUNT(*) FROM facts_sales GROUP BY HOUR(sale_time)", "difficulty": "medium", "tags": ["时间函数", "聚合分组"]},
  {"id": 33, "question": "门店名称和所属区域", "gold_sql": "SELECT shop_name, region FROM dims_shop", "difficulty": "medium", "tags": ["单表查询"]},
  {"id": 34, "question": "商品名称和一级品类", "gold_sql": "SELECT product_name, category FROM dims_product", "difficulty": "medium", "tags": ["单表查询"]},
  {"id": 35, "question": "销售记录中的商品名称", "gold_sql": "SELECT fs.*, dp.product_name FROM facts_sales fs JOIN dims_product dp ON fs.product_id=dp.product_id", "difficulty": "medium", "tags": ["多表JOIN"]},
  {"id": 36, "question": "销售记录中的门店名称", "gold_sql": "SELECT fs.*, ds.shop_name FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id", "difficulty": "medium", "tags": ["多表JOIN"]},
  {"id": 37, "question": "完整的销售明细", "gold_sql": "SELECT fs.*, ds.shop_name, dp.product_name FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id JOIN dims_product dp ON fs.product_id=dp.product_id", "difficulty": "medium", "tags": ["多表JOIN"]},
  {"id": 38, "question": "华东区门店的销售记录", "gold_sql": "SELECT fs.* FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id WHERE ds.region='华东'", "difficulty": "medium", "tags": ["多表JOIN", "条件过滤"]},
  {"id": 39, "question": "电子数码类商品的销售记录", "gold_sql": "SELECT fs.* FROM facts_sales fs JOIN dims_product dp ON fs.product_id=dp.product_id WHERE dp.category='电子数码'", "difficulty": "medium", "tags": ["多表JOIN", "条件过滤"]},
  {"id": 40, "question": "杭州门店的销售记录", "gold_sql": "SELECT fs.* FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id WHERE ds.city='杭州'", "difficulty": "medium", "tags": ["多表JOIN", "条件过滤"]},
  {"id": 41, "question": "各门店名称及其销售总额", "gold_sql": "SELECT ds.shop_name, SUM(fs.total_amount) FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id GROUP BY ds.shop_name", "difficulty": "medium", "tags": ["多表JOIN", "聚合分组"]},
  {"id": 42, "question": "各商品名称及其销售总量", "gold_sql": "SELECT dp.product_name, SUM(fs.quantity) FROM facts_sales fs JOIN dims_product dp ON fs.product_id=dp.product_id GROUP BY dp.product_name", "difficulty": "medium", "tags": ["多表JOIN", "聚合分组"]},
  {"id": 43, "question": "各区域的销售总额", "gold_sql": "SELECT ds.region, SUM(fs.total_amount) FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id GROUP BY ds.region", "difficulty": "medium", "tags": ["多表JOIN", "聚合分组"]},
  {"id": 44, "question": "各品类的销售总额", "gold_sql": "SELECT dp.category, SUM(fs.total_amount) FROM facts_sales fs JOIN dims_product dp ON fs.product_id=dp.product_id GROUP BY dp.category", "difficulty": "medium", "tags": ["多表JOIN", "聚合分组"]},
  {"id": 45, "question": "各城市的销售总额", "gold_sql": "SELECT ds.city, SUM(fs.total_amount) FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id GROUP BY ds.city", "difficulty": "medium", "tags": ["多表JOIN", "聚合分组"]},
  {"id": 46, "question": "各门店的平均单笔销售额", "gold_sql": "SELECT ds.shop_name, AVG(fs.total_amount) FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id GROUP BY ds.shop_name", "difficulty": "medium", "tags": ["多表JOIN", "聚合分组"]},
  {"id": 47, "question": "各商品的平均销售数量", "gold_sql": "SELECT dp.product_name, AVG(fs.quantity) FROM facts_sales fs JOIN dims_product dp ON fs.product_id=dp.product_id GROUP BY dp.product_name", "difficulty": "medium", "tags": ["多表JOIN", "聚合分组"]},
  {"id": 48, "question": "华东区各门店的销售总额", "gold_sql": "SELECT ds.shop_name, SUM(fs.total_amount) FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id WHERE ds.region='华东' GROUP BY ds.shop_name", "difficulty": "medium", "tags": ["多表JOIN", "条件过滤", "聚合分组"]},
  {"id": 49, "question": "电子数码类各商品的销售总量", "gold_sql": "SELECT dp.product_name, SUM(fs.quantity) FROM facts_sales fs JOIN dims_product dp ON fs.product_id=dp.product_id WHERE dp.category='电子数码' GROUP BY dp.product_name", "difficulty": "medium", "tags": ["多表JOIN", "条件过滤", "聚合分组"]},
  {"id": 50, "question": "6月份各门店的销售总额", "gold_sql": "SELECT ds.shop_name, SUM(fs.total_amount) FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id WHERE fs.sale_time >= '2026-06-01' AND fs.sale_time < '2026-07-01' GROUP BY ds.shop_name", "difficulty": "medium", "tags": ["多表JOIN", "时间函数", "聚合分组"]},
  {"id": 51, "question": "销售额最高的Top3门店", "gold_sql": "SELECT ds.shop_name, SUM(fs.total_amount) FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id GROUP BY ds.shop_name ORDER BY SUM(fs.total_amount) DESC LIMIT 3", "difficulty": "complex", "tags": ["TopN", "多表JOIN", "聚合分组"]},
  {"id": 52, "question": "销量最高的Top5商品", "gold_sql": "SELECT dp.product_name, SUM(fs.quantity) FROM facts_sales fs JOIN dims_product dp ON fs.product_id=dp.product_id GROUP BY dp.product_name ORDER BY SUM(fs.quantity) DESC LIMIT 5", "difficulty": "complex", "tags": ["TopN", "多表JOIN", "聚合分组"]},
  {"id": 53, "question": "华东区销售额最高的Top3门店", "gold_sql": "SELECT ds.shop_name, SUM(fs.total_amount) FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id WHERE ds.region='华东' GROUP BY ds.shop_name ORDER BY SUM(fs.total_amount) DESC LIMIT 3", "difficulty": "complex", "tags": ["TopN", "多表JOIN", "条件过滤", "聚合分组"]},
  {"id": 54, "question": "6月份各区域销售冠军门店", "gold_sql": "SELECT region, shop_name, total_sales FROM (SELECT ds.region, ds.shop_name, SUM(fs.total_amount) total_sales, ROW_NUMBER() OVER(PARTITION BY ds.region ORDER BY SUM(fs.total_amount) DESC) rn FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id WHERE fs.sale_time >= '2026-06-01' AND fs.sale_time < '2026-07-01' GROUP BY ds.region, ds.shop_name) t WHERE rn=1", "difficulty": "complex", "tags": ["TopN", "窗口函数", "多表JOIN", "时间函数"]},
  {"id": 55, "question": "各品类销量最高的商品", "gold_sql": "SELECT category, product_name, total_qty FROM (SELECT dp.category, dp.product_name, SUM(fs.quantity) total_qty, ROW_NUMBER() OVER(PARTITION BY dp.category ORDER BY SUM(fs.quantity) DESC) rn FROM facts_sales fs JOIN dims_product dp ON fs.product_id=dp.product_id GROUP BY dp.category, dp.product_name) t WHERE rn=1", "difficulty": "complex", "tags": ["TopN", "窗口函数", "多表JOIN"]},
  {"id": 56, "question": "销售总额超过10万的门店", "gold_sql": "SELECT ds.shop_name, SUM(fs.total_amount) FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id GROUP BY ds.shop_name HAVING SUM(fs.total_amount) > 100000", "difficulty": "complex", "tags": ["HAVING过滤", "多表JOIN", "聚合分组"]},
  {"id": 57, "question": "销售总量超过100的商品", "gold_sql": "SELECT dp.product_name, SUM(fs.quantity) FROM facts_sales fs JOIN dims_product dp ON fs.product_id=dp.product_id GROUP BY dp.product_name HAVING SUM(fs.quantity) > 100", "difficulty": "complex", "tags": ["HAVING过滤", "多表JOIN", "聚合分组"]},
  {"id": 58, "question": "平均单笔销售额超过5000的门店", "gold_sql": "SELECT ds.shop_name, AVG(fs.total_amount) FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id GROUP BY ds.shop_name HAVING AVG(fs.total_amount) > 5000", "difficulty": "complex", "tags": ["HAVING过滤", "多表JOIN", "聚合分组"]},
  {"id": 59, "question": "各门店的折扣率", "gold_sql": "SELECT ds.shop_name, SUM(fs.discount_amount)/SUM(fs.price*fs.quantity) discount_rate FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id GROUP BY ds.shop_name", "difficulty": "complex", "tags": ["多表JOIN", "聚合分组", "计算字段"]},
  {"id": 60, "question": "各商品的毛利率", "gold_sql": "SELECT dp.product_name, SUM(fs.total_amount - dp.cost*fs.quantity)/SUM(fs.total_amount) gross_margin FROM facts_sales fs JOIN dims_product dp ON fs.product_id=dp.product_id GROUP BY dp.product_name", "difficulty": "complex", "tags": ["多表JOIN", "聚合分组", "计算字段"]},
  {"id": 61, "question": "6月15日到6月30日的销售总额", "gold_sql": "SELECT SUM(total_amount) FROM facts_sales WHERE sale_time >= '2026-06-15' AND sale_time < '2026-07-01'", "difficulty": "complex", "tags": ["时间函数", "日期范围"]},
  {"id": 62, "question": "周末的销售总额", "gold_sql": "SELECT SUM(total_amount) FROM facts_sales WHERE DAYOFWEEK(sale_time) IN (1,7)", "difficulty": "complex", "tags": ["时间函数", "条件过滤"]},
  {"id": 63, "question": "工作日的销售总额", "gold_sql": "SELECT SUM(total_amount) FROM facts_sales WHERE DAYOFWEEK(sale_time) BETWEEN 2 AND 6", "difficulty": "complex", "tags": ["时间函数", "条件过滤"]},
  {"id": 64, "question": "上午（9点到12点）的销售总额", "gold_sql": "SELECT SUM(total_amount) FROM facts_sales WHERE HOUR(sale_time) BETWEEN 9 AND 12", "difficulty": "complex", "tags": ["时间函数", "条件过滤"]},
  {"id": 65, "question": "下午（14点到18点）的销售总额", "gold_sql": "SELECT SUM(total_amount) FROM facts_sales WHERE HOUR(sale_time) BETWEEN 14 AND 18", "difficulty": "complex", "tags": ["时间函数", "条件过滤"]},
  {"id": 66, "question": "晚间（19点到22点）的销售总额", "gold_sql": "SELECT SUM(total_amount) FROM facts_sales WHERE HOUR(sale_time) BETWEEN 19 AND 22", "difficulty": "complex", "tags": ["时间函数", "条件过滤"]},
  {"id": 67, "question": "上个月的销售总额", "gold_sql": "SELECT SUM(total_amount) FROM facts_sales WHERE DATE_FORMAT(sale_time, '%Y-%m') = DATE_FORMAT(DATE_SUB(CURDATE(), INTERVAL 1 MONTH), '%Y-%m')", "difficulty": "complex", "tags": ["时间函数", "相对时间"]},
  {"id": 68, "question": "本月的销售总额", "gold_sql": "SELECT SUM(total_amount) FROM facts_sales WHERE DATE_FORMAT(sale_time, '%Y-%m') = DATE_FORMAT(CURDATE(), '%Y-%m')", "difficulty": "complex", "tags": ["时间函数", "相对时间"]},
  {"id": 69, "question": "上周的销售总额", "gold_sql": "SELECT SUM(total_amount) FROM facts_sales WHERE YEARWEEK(DATE(sale_time), 1) = YEARWEEK(CURDATE(), 1) - 1", "difficulty": "complex", "tags": ["时间函数", "相对时间"]},
  {"id": 70, "question": "本周的销售总额", "gold_sql": "SELECT SUM(total_amount) FROM facts_sales WHERE YEARWEEK(DATE(sale_time), 1) = YEARWEEK(CURDATE(), 1)", "difficulty": "complex", "tags": ["时间函数", "相对时间"]},
  {"id": 71, "question": "销售记录中包含手机的订单", "gold_sql": "SELECT DISTINCT order_id FROM facts_sales fs JOIN dims_product dp ON fs.product_id=dp.product_id WHERE dp.subcategory='手机'", "difficulty": "complex", "tags": ["子查询", "多表JOIN"]},
  {"id": 72, "question": "同时购买了手机和配件的订单", "gold_sql": "SELECT order_id FROM facts_sales fs JOIN dims_product dp ON fs.product_id=dp.product_id WHERE dp.subcategory='手机' AND order_id IN (SELECT order_id FROM facts_sales fs2 JOIN dims_product dp2 ON fs2.product_id=dp2.product_id WHERE dp2.subcategory='配件')", "difficulty": "complex", "tags": ["子查询", "多表JOIN"]},
  {"id": 73, "question": "只购买了电子数码类商品的订单", "gold_sql": "SELECT order_id FROM facts_sales fs JOIN dims_product dp ON fs.product_id=dp.product_id WHERE dp.category='电子数码' GROUP BY order_id HAVING COUNT(DISTINCT dp.category) = 1", "difficulty": "complex", "tags": ["子查询", "HAVING过滤", "多表JOIN"]},
  {"id": 74, "question": "购买了多种不同品类商品的订单", "gold_sql": "SELECT order_id, COUNT(DISTINCT dp.category) FROM facts_sales fs JOIN dims_product dp ON fs.product_id=dp.product_id GROUP BY order_id HAVING COUNT(DISTINCT dp.category) > 1", "difficulty": "complex", "tags": ["HAVING过滤", "多表JOIN"]},
  {"id": 75, "question": "各门店的订单数排名", "gold_sql": "SELECT ds.shop_name, COUNT(DISTINCT fs.order_id) order_count, RANK() OVER(ORDER BY COUNT(DISTINCT fs.order_id) DESC) rank FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id GROUP BY ds.shop_name", "difficulty": "complex", "tags": ["窗口函数", "多表JOIN", "聚合分组"]},
  {"id": 76, "question": "各商品的销售额占比", "gold_sql": "SELECT dp.product_name, SUM(fs.total_amount), SUM(fs.total_amount)/(SELECT SUM(total_amount) FROM facts_sales) ratio FROM facts_sales fs JOIN dims_product dp ON fs.product_id=dp.product_id GROUP BY dp.product_name", "difficulty": "complex", "tags": ["子查询", "多表JOIN", "聚合分组"]},
  {"id": 77, "question": "各区域的销售额占比", "gold_sql": "SELECT ds.region, SUM(fs.total_amount), SUM(fs.total_amount)/(SELECT SUM(total_amount) FROM facts_sales) ratio FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id GROUP BY ds.region", "difficulty": "complex", "tags": ["子查询", "多表JOIN", "聚合分组"]},
  {"id": 78, "question": "每个门店销售的商品种类数", "gold_sql": "SELECT ds.shop_name, COUNT(DISTINCT fs.product_id) FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id GROUP BY ds.shop_name", "difficulty": "complex", "tags": ["多表JOIN", "聚合分组", "去重计数"]},
  {"id": 79, "question": "每个商品在多少个门店销售过", "gold_sql": "SELECT dp.product_name, COUNT(DISTINCT fs.shop_id) FROM facts_sales fs JOIN dims_product dp ON fs.product_id=dp.product_id GROUP BY dp.product_name", "difficulty": "complex", "tags": ["多表JOIN", "聚合分组", "去重计数"]},
  {"id": 80, "question": "没有销售记录的门店", "gold_sql": "SELECT ds.shop_name FROM dims_shop ds LEFT JOIN facts_sales fs ON ds.shop_id=fs.shop_id WHERE fs.shop_id IS NULL", "difficulty": "complex", "tags": ["LEFT JOIN", "空值判断"]},
  {"id": 81, "question": "没有销售记录的商品", "gold_sql": "SELECT dp.product_name FROM dims_product dp LEFT JOIN facts_sales fs ON dp.product_id=fs.product_id WHERE fs.product_id IS NULL", "difficulty": "complex", "tags": ["LEFT JOIN", "空值判断"]},
  {"id": 82, "question": "各门店的客单价", "gold_sql": "SELECT ds.shop_name, SUM(fs.total_amount)/COUNT(DISTINCT fs.order_id) avg_order_amount FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id GROUP BY ds.shop_name", "difficulty": "complex", "tags": ["多表JOIN", "聚合分组", "计算字段"]},
  {"id": 83, "question": "各品类的客单价", "gold_sql": "SELECT dp.category, SUM(fs.total_amount)/COUNT(DISTINCT fs.order_id) avg_order_amount FROM facts_sales fs JOIN dims_product dp ON fs.product_id=dp.product_id GROUP BY dp.category", "difficulty": "complex", "tags": ["多表JOIN", "聚合分组", "计算字段"]},
  {"id": 84, "question": "6月份华东区各门店的销售趋势", "gold_sql": "SELECT DATE(fs.sale_time), ds.shop_name, SUM(fs.total_amount) FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id WHERE ds.region='华东' AND fs.sale_time >= '2026-06-01' AND fs.sale_time < '2026-07-01' GROUP BY DATE(fs.sale_time), ds.shop_name ORDER BY DATE(fs.sale_time), ds.shop_name", "difficulty": "complex", "tags": ["多表JOIN", "时间函数", "聚合分组"]},
  {"id": 85, "question": "各门店的销售峰值日期", "gold_sql": "SELECT shop_name, sale_date, total_sales FROM (SELECT ds.shop_name, DATE(fs.sale_time) sale_date, SUM(fs.total_amount) total_sales, ROW_NUMBER() OVER(PARTITION BY ds.shop_name ORDER BY SUM(fs.total_amount) DESC) rn FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id GROUP BY ds.shop_name, DATE(fs.sale_time)) t WHERE rn=1", "difficulty": "complex", "tags": ["窗口函数", "多表JOIN", "时间函数"]},
  {"id": 86, "question": "各商品的销售峰值日期", "gold_sql": "SELECT product_name, sale_date, total_qty FROM (SELECT dp.product_name, DATE(fs.sale_time) sale_date, SUM(fs.quantity) total_qty, ROW_NUMBER() OVER(PARTITION BY dp.product_name ORDER BY SUM(fs.quantity) DESC) rn FROM facts_sales fs JOIN dims_product dp ON fs.product_id=dp.product_id GROUP BY dp.product_name, DATE(fs.sale_time)) t WHERE rn=1", "difficulty": "complex", "tags": ["窗口函数", "多表JOIN", "时间函数"]},
  {"id": 87, "question": "折扣金额最高的订单", "gold_sql": "SELECT order_id, SUM(discount_amount) FROM facts_sales GROUP BY order_id ORDER BY SUM(discount_amount) DESC LIMIT 1", "difficulty": "complex", "tags": ["聚合分组", "排序"]},
  {"id": 88, "question": "平均折扣率最高的商品", "gold_sql": "SELECT dp.product_name, SUM(fs.discount_amount)/SUM(fs.price*fs.quantity) avg_discount_rate FROM facts_sales fs JOIN dims_product dp ON fs.product_id=dp.product_id GROUP BY dp.product_name ORDER BY avg_discount_rate DESC LIMIT 1", "difficulty": "complex", "tags": ["多表JOIN", "聚合分组", "计算字段"]},
  {"id": 89, "question": "各门店的最大订单金额", "gold_sql": "SELECT ds.shop_name, MAX(order_total) FROM (SELECT ds.shop_name, fs.order_id, SUM(fs.total_amount) order_total FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id GROUP BY ds.shop_name, fs.order_id) t GROUP BY ds.shop_name", "difficulty": "complex", "tags": ["子查询", "多表JOIN", "聚合分组"]},
  {"id": 90, "question": "各门店的最小订单金额", "gold_sql": "SELECT ds.shop_name, MIN(order_total) FROM (SELECT ds.shop_name, fs.order_id, SUM(fs.total_amount) order_total FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id GROUP BY ds.shop_name, fs.order_id) t GROUP BY ds.shop_name", "difficulty": "complex", "tags": ["子查询", "多表JOIN", "聚合分组"]},
  {"id": 91, "question": "6月份和7月份的销售对比", "gold_sql": "SELECT CASE WHEN sale_time >= '2026-07-01' THEN '7月' ELSE '6月' END month, SUM(total_amount) FROM facts_sales WHERE sale_time >= '2026-06-01' AND sale_time < '2026-08-01' GROUP BY month", "difficulty": "complex", "tags": ["时间函数", "条件分组", "聚合分组"]},
  {"id": 92, "question": "各区域6月份和7月份的销售对比", "gold_sql": "SELECT ds.region, CASE WHEN fs.sale_time >= '2026-07-01' THEN '7月' ELSE '6月' END month, SUM(fs.total_amount) FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id WHERE fs.sale_time >= '2026-06-01' AND fs.sale_time < '2026-08-01' GROUP BY ds.region, month", "difficulty": "complex", "tags": ["多表JOIN", "时间函数", "条件分组", "聚合分组"]},
  {"id": 93, "question": "销售金额波动最大的门店", "gold_sql": "SELECT ds.shop_name, STDDEV(fs.total_amount) FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id GROUP BY ds.shop_name ORDER BY STDDEV(fs.total_amount) DESC LIMIT 1", "difficulty": "complex", "tags": ["多表JOIN", "聚合分组", "统计函数"]},
  {"id": 94, "question": "销售数量波动最大的商品", "gold_sql": "SELECT dp.product_name, STDDEV(fs.quantity) FROM facts_sales fs JOIN dims_product dp ON fs.product_id=dp.product_id GROUP BY dp.product_name ORDER BY STDDEV(fs.quantity) DESC LIMIT 1", "difficulty": "complex", "tags": ["多表JOIN", "聚合分组", "统计函数"]},
  {"id": 95, "question": "各门店的销售天数", "gold_sql": "SELECT ds.shop_name, COUNT(DISTINCT DATE(fs.sale_time)) FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id GROUP BY ds.shop_name", "difficulty": "complex", "tags": ["多表JOIN", "聚合分组", "时间函数"]},
  {"id": 96, "question": "各商品的销售天数", "gold_sql": "SELECT dp.product_name, COUNT(DISTINCT DATE(fs.sale_time)) FROM facts_sales fs JOIN dims_product dp ON fs.product_id=dp.product_id GROUP BY dp.product_name", "difficulty": "complex", "tags": ["多表JOIN", "聚合分组", "时间函数"]},
  {"id": 97, "question": "每天的订单数量", "gold_sql": "SELECT DATE(sale_time), COUNT(DISTINCT order_id) FROM facts_sales GROUP BY DATE(sale_time)", "difficulty": "complex", "tags": ["时间函数", "聚合分组", "去重计数"]},
  {"id": 98, "question": "每天平均每笔订单的商品数量", "gold_sql": "SELECT DATE(sale_time), SUM(quantity)/COUNT(DISTINCT order_id) avg_items_per_order FROM facts_sales GROUP BY DATE(sale_time)", "difficulty": "complex", "tags": ["时间函数", "聚合分组", "计算字段"]},
  {"id": 99, "question": "各门店平均每天的销售额", "gold_sql": "SELECT ds.shop_name, SUM(fs.total_amount)/COUNT(DISTINCT DATE(fs.sale_time)) avg_daily_sales FROM facts_sales fs JOIN dims_shop ds ON fs.shop_id=ds.shop_id GROUP BY ds.shop_name", "difficulty": "complex", "tags": ["多表JOIN", "聚合分组", "时间函数", "计算字段"]},
  {"id": 100, "question": "各品类平均每天的销量", "gold_sql": "SELECT dp.category, SUM(fs.quantity)/COUNT(DISTINCT DATE(fs.sale_time)) avg_daily_qty FROM facts_sales fs JOIN dims_product dp ON fs.product_id=dp.product_id GROUP BY dp.category", "difficulty": "complex", "tags": ["多表JOIN", "聚合分组", "时间函数", "计算字段"]}
]